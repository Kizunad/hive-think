---
description: Deep multi-model parallel thinking for complex decisions — spawns multiple models with full context, returns all perspectives side-by-side
---
Use the hive_think tool to deeply analyze the following question or problem:

$@

**When calling hive_think:**
- Pass the complete question as the `question` parameter
- Include relevant conversation context, constraints, code, and anything else the models need in the `context` parameter
- The tool will spawn multiple models in parallel with full context, all thinking independently
- Review and compare all perspectives before making a final decision
